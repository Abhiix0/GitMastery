# Challenge — m07

Create a conflict on purpose, but this time on a function.

Two branches, both modifying the `updateScore` function in `index.js` differently — one adds input validation, the other changes how the score is calculated.

Resolve the conflict so **both changes are preserved** and the function still works correctly. Open the game in a browser after resolving to verify nothing is broken.
