# Challenge — m06

You have a branch with 5 commits. Three of them are real work. Two of them are "oops" fixes for mistakes in the real work.

Merge it into main so the history shows only the meaningful commits — no "wip", no "fix typo", no noise.

No interactive rebase. Just the right merge strategy.

Show `git log --oneline` after.
