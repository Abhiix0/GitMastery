# Challenge — m08

You're mid-feature. Stash your work.

Switch context, make an urgent commit on main, and tag it as a hotfix release with an annotated tag.

Then restore your stash and continue.

Also: add a `.gitignore` that covers these three cases:
- `.DS_Store` files (macOS noise)
- The entire `node_modules/` directory
- Any file ending in `.log`

Show `git tag` and `git stash list` at the end.
