# Challenge — m05

Create two branches from main at the same time. Make different changes on each:

- One branch adds a comment to `index.js`
- The other edits the title in `index.html`

Merge both into main. If you picked your changes carefully, there won't be any conflicts.

Show the result with `git log --oneline --graph`. Then delete both branches.
