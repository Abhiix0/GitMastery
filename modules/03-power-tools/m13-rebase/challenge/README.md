# Challenge — m13

You have a branch with 7 commits. Use interactive rebase to reduce it to 3 — each with a meaningful message that tells a clear story.

Then rebase that cleaned branch onto the latest main.

Then fast-forward merge it into main.

Show `git log --oneline` before the interactive rebase and after the final merge. The difference should be obvious.
