# Challenge — m01

Your git config is set globally. Now figure out how to set a **different** name and email just for one specific project — without touching your global config.

The change should only apply inside that repo. Your global config stays the same everywhere else.

No steps given. Check `git config --help` if you're stuck on the flag name.
