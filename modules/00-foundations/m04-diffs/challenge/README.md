# Challenge — m04

You have at least two commits in your repo. Without checking either one out, figure out what changed between them — every line that was added or removed.

Then: make changes to 3 different files but only stage 2 of them. Run the right diff commands to prove you know the exact state of all 3 files — what's staged, what's not, and what the full picture looks like.
